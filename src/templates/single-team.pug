extends inc/page.pug
block template
  :php
    /*
    Template Name: Team Single Entry Page
    */
block nav
  - var header = "white";
  include ./inc/navbar-toggle.pug
  include ./inc/side-nav.pug
  include ./inc/header.pug
block content
  :php
    if( have_posts() ){
     while( have_posts() ) {
      the_post();
  section.container.blog-content.padding-top-oldpage
    .row
      .col-md-12
        article.blog-post
          .blog-post-body
            :php
              the_content();
        :php
          }}
