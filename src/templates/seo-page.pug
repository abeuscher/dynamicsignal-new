extends inc/page.pug
block template
  :php
    /*
    Template Name: SEO Page
    */
block nav
  include ./inc/navbar-toggle.pug
  include ./inc/side-nav.pug
  include ./inc/header.pug
block content
    :php
        printf("<section class='seo-header' data-bg-array='%s'>", json_encode(get_field("header_image"), JSON_UNESCAPED_SLASHES));
    .container
        .row
            .col-md-6
                :php
                    printf("<h1 class='caps'>%s</h1>", get_field("header_title"));
    :php
       echo "</section>";
    :php
        foreach(get_field("sections") as $idx=>$this_section) {
            
        }