extends inc/page.pug
block template
  :php
    /*
    Template Name: SnapApp Outcome Page
    */
block nav
  include ./inc/navbar-toggle.pug
  include ./inc/side-nav.pug
  include ./inc/header.pug
block content
  section.snapapp-header(data-bg="bg-snapapp-header.jpg")
    .container
        .row
            .col-md-12
              h1 Take the 
                span.blue Pulse 
                | of Your Employee Engagement
  section
    .container
      .row
        .col-md-12
          :php
            if( have_posts() ){
            while( have_posts() ) {
              the_post();
              the_content();
            }}
block modals
