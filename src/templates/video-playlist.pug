extends inc/page.pug
block template
  :php
    /*
    Template Name: Video Playlist
    */
block nav
  include ./inc/navbar-toggle.pug
  include ./inc/side-nav.pug
  include ./inc/header.pug
block content
  :php
    if( have_posts() ){
    while( have_posts() ) {
    the_post();

    /* Write video data */
    echo "<script>var pageData = {'videos':".json_encode(get_field("videos"),JSON_UNESCAPED_SLASHES)
    ."};</script>";
  section.top-section-interior
    .container
      .row
        .col-md-12
          h1
            :php
              the_title();
  :php
    }}
  section.container
    .row
      .col-md-12
        #demo-videos

        
