- var className = homepage ? 'page-header homepage' : 'page-header';
- className += header=="white" ? " white" : "";
header#page-header(class=className)
  :php
    global $post;
    $post_slug=$post->post_name;
    $pages = array(
      "product"=>"Platform",
      "solutions"=>"Solutions",
      "customers"=>"Customers",
      "company"=>"Company",
      "resources"=>"Resources",
      "requestdemo"=>"Request a Demo"
    );
    $page_classes = [];
    foreach($pages as $key=>$page) {
      $page_classes[] = ($key == $post_slug) ? $key." active" : $key;
    }
  nav.main
    a.logo-bucket(href="/")
      include ../../templates/inc/logo.pug
    ul.hmenu.top-nav
      :php
        printf("<li class='has-drop %s'>", $page_classes[0]);
      a(href="/solutions/") Solutions
      ul.drop
        li
          a(href="/workforce-communication/") Communication
        li
          a(href="/employee-experience/") Human Resources
        li
          a(href="/solution-employee-advocacy/") Marketing
        li
          a(href="/employee-advocacy/") Employee Advocacy
        li
          hr
        li
          a(href="https://resources.dynamicsignal.com/healthcare") Healthcare
        li
          a(href="https://resources.dynamicsignal.com/manufacturing") Manufacturing
        li
          a(href="https://resources.dynamicsignal.com/retail") Retail  
      :php
        echo "</li>";
        printf("<li class='%s'>", $page_classes[2]);
      a(href="/blog/") Blog  
      :php
        echo "</li>";
        printf("<li class='%s'>", $page_classes[3]);
      a(href="/contact/") Contact
      :php
        echo "</li>";
        printf("<li class='%s show-medium'>", $page_classes[5]);
      a.request-demo.cta(href="/requestdemo/") Request a Demo
      :php
        echo "</li>";
      li.show-mobile
        a.hamburger#toggle-side-nav(href="#",title="menu")
          .line.top
          .line.middle
          .line.bottom